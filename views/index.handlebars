<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>BURGER WORLD </h1>
            <div>
                <form action="/" method="POST">
                    <textarea type="text" name="burger_type"></textarea>

                    <button type="submit">Submit</button>
                </form>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">

            <ul>
                {{#each burgers}}
                <li>
                    <p>ID: {{this.id}}</p>
                    <p>burger: {{this.burger_name}}</p>
                </li>
                {{/each}}
            </ul>
        </div>
        <div class="col-md-6">

        </div>

    </div>
</div>

<ul> {{#each burgers}}
    <li>
        <p>ID: {{this.id}}</p>
        <p>Wish: {{this.burgername}}</p>
    </li>
    {{/each}}
</ul>